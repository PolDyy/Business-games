from django.apps import AppConfig


class AuthenticateConfig(AppConfig):
    """Класс Django приложения."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.authenticate"
    label = "authenticate"
